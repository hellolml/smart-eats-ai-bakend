# SmartEats 架构升级复盘（详细技术版）

> 版本范围：从引入原生 Function Calling 到当前 SmartEats 全链路重构
> 目标读者：研发团队、架构评审、技术复盘会议

---

## 1. 背景与问题定义

本次重构前，系统处于“可用但不稳定优化”的阶段，典型问题包括：

1. **调用链路不够确定**
   - 工具调用在文本解析链路上存在格式敏感性；
   - 模型输出轻微波动会放大成调用失败、补救重试、或分支漂移。

2. **控制流复杂度高**
   - `if/else` 逻辑跨语义域（route/recipe/restaurant）分散；
   - 业务意图、工具后处理、最终输出拼装耦合，定位问题成本高。

3. **工具结果消费链路存在损耗**
   - 出现“工具成功、日志可见，但最终答复未稳定使用关键字段”的现象；
   - 特别在路线场景，结果传递到 final answer 的一致性不足。

4. **输出层不够统一**
   - `final_json` 在多个分支拼装，风格和结构的一致性受实现路径影响；
   - fallback 在部分场景呈现机械化。

**重构目标**：在不改变业务语义的前提下，提升链路确定性、状态一致性、输出稳定性与回归可控性。

---

## 2. 总体技术路线（从“弱约束文本代理”到“强契约结构化代理”）

本次升级不是单点优化，而是分层推进：

1. **协议层**：原生 Function Calling 替代文本式工具调用解析。
2. **运行时层**：构建 SmartEats 专属 Graph（observe/think/tools/postprocess/finalize）。
3. **控制层**：将分支树重构为分发表/策略映射。
4. **状态层**：关键事实显式进入上下文主路径（如 `context.latest_route`）。
5. **输出层**：统一 final answer builder 与模板入口。
6. **验证层**：新增关键链路 E2E 防回归用例。

---

## 3. 分层改造细节与原理

### 3.1 协议层：引入原生 Function Calling

**改造要点**
- 让模型直接返回结构化 `tool_calls`，减少对文本后解析的依赖。

**原理**
- 使用 schema 约束替代自然语言约定，降低调用入口的不确定性。

**收益**
- 调用成功率提升；
- 因格式偏差导致的重试与补救下降。

---

### 3.2 运行时层：Graph-first 的 SmartEats 专属链路

**改造要点**
- 固化阶段：`observe -> think -> tools -> tool_postprocess -> finalize`；
- 将通用/legacy 逻辑与 smart_eats 业务链路解耦。

**原理**
- 职责边界显式化，状态演进路径可观测，降低跨阶段副作用。

**收益**
- 调试与定位效率提升；
- 路径更短，运行开销更可控。

---

### 3.3 控制层：从 if/else 转向分发表与策略映射

**改造要点**
- `_TOOL_RESULT_DISPATCH` 将工具结果处理映射化；
- route 错误分支采用 `_PLAN_ROUTE_ERROR_HANDLERS` 映射。

**原理**
- 声明式路由替代隐式条件树，降低分支冲突与漏改风险。

**收益**
- 行为一致性上升；
- 可维护性和可扩展性提升。

---

### 3.4 状态层：关键事实上浮（latest_route + system_directive）

**改造要点**
- 在 `plan_route` 成功后，将关键字段注入 `context.latest_route`；
- 同步注入 `context.system_directive`，约束“信息足够时立即 submit_final_answer”；
- Prompt 增加约束：当 `context.latest_route` 存在，路线答复必须优先使用。

**原理**
- 将“工具观察结果”提升为“高优先级决策输入”，减少事实在多轮中的传递损耗。

**收益**
- 明显降低“日志有结果、最终答复未消费”的概率；
- 路线场景首答准确性提升。

---

### 3.5 输出层：统一 FinalAnswer Builder + 模板化入口

**改造要点**
- 统一 `_build_final_answer` / `_build_single_note_final_answer` / `_build_note_template_final_answer`；
- note、route 错误、clarify 等分支由模板驱动。

**原理**
- 输出结构标准化，减少多点拼装造成的风格/字段漂移。

**收益**
- 响应文本更连贯；
- 响应结构更稳定，前端渲染一致性更高。

---

### 3.6 兜底层：best-effort 由“机械兜底”改为“观察驱动兜底”

**改造要点**
- 基于 observations 扫描策略提取最后可用事实，再进行策略化兜底输出。

**原理**
- 兜底不再脱离上下文，而是最大化复用已获取事实。

**收益**
- fallback 生硬感降低；
- 用户主观感受更“丝滑”。

---

### 3.7 测试层：关键链路 E2E 防回归

**改造要点**
- 新增 route 端到端场景：
  1) 第一轮 planner 调 `plan_route`；
  2) 第二轮 planner 校验可从 context 读取 `latest_route`；
  3) `submit_final_answer` 必须包含路线关键字段。

**原理**
- 把“体验要求”转化为可执行断言，避免回归靠人工感知。

**收益**
- 关键场景退化可快速暴露、快速定位。

---

## 4. 为什么这次会出现“速度 + 稳定 + 体验”同步提升

可将端到端耗时抽象为：

`总时延 = planner轮次成本 + 工具调用成本 + 重试/补救成本 + 输出修复成本`

本次重构主要降低了后三项：

1. **重试/补救成本下降**
   - Function Calling + 参数归一化减少无效调用；
2. **输出修复成本下降**
   - latest_route + directive + prompt 约束，减少首答缺关键信息后的二次追问；
3. **调用成本下降**
   - 收敛策略减少无效探索与重复工具调用。

因此出现“更快 + 更稳 + 更自然”的叠加体验。

---

## 5. 关键能力前后对照

| 能力 | 改造前 | 改造后 |
|---|---|---|
| 工具调用确定性 | 中（依赖文本解析） | 高（原生 Function Calling） |
| 关键事实传递 | 可能丢失 | 显式上下文注入（latest_route） |
| 路由可解释性 | 低（分支散） | 高（分发表 + 策略映射） |
| 输出一致性 | 中 | 高（统一 builder + 模板） |
| 回归防线 | 局部断言 | 覆盖关键链路的 E2E |

---

## 6. 指标体系与观测模板

### 6.1 建议采集字段
- `planner_round_count`
- `tool_call_count`
- `is_fallback_final`
- `route_fields_covered`（distance/duration/steps）
- `time_to_first_final_ms`
- `first_answer_hit`

### 6.2 KPI 模板（按 scene 分桶统计）

| 指标 | 定义口径 | 改造前 | 改造后 | 提升幅度 |
|---|---|---:|---:|---:|
| 平均轮次 | 每请求到首个 final 的 planner 轮数均值 | 待填 | 待填 | 待填 |
| 平均工具调用数 | 每请求触发 tool_calls 数均值 | 待填 | 待填 | 待填 |
| 首答命中率 | 首个 final 满足关键场景信息需求比例 | 待填 | 待填 | 待填 |
| fallback率 | 进入 fallback 分支占比 | 待填 | 待填 | 待填 |
| 路线结果利用率 | route 场景中 final 使用关键路线字段比例 | 待填 | 待填 | 待填 |
| 首答时延P50/P95 | 请求到 final 的时延分位 | 待填 | 待填 | 待填 |

### 6.3 评估建议
- 统计窗口：改造前后各 7~14 天；
- 分场景：`route / eat_out / cook_home / chat`；
- 分层看：质量（命中率）、效率（轮次/时延）、稳定（回归通过率）。

---

## 7. 代表性技术落点（供代码走查）

- 路线结果上下文消费约束：
  - `app/agent/prompts/system.md`
- SmartEats 专属链路与后处理：
  - `app/agent/agents/smart_eats.py`
- route 关键链路 E2E 回归：
  - `app/tests/test_smart_eats_graph_builder.py`

---

## 8. 风险与后续建议

### 8.1 已显著缓解风险
1. 关键工具结果未进入最终答复
2. 分支漂移导致的偶发错误
3. 输出结构不一致导致的用户体感波动

### 8.2 后续演进建议
1. 指标接入线上看板，持续验证改造收益；
2. 为 route 场景加入字段覆盖率告警（缺距离/时长/步骤时报警）；
3. 将高失败样本纳入自动回放回归集；
4. 延续“常量化 + 策略化 + 模板化”路线，持续压缩隐式分支。

---

## 9. 技术结论

本次重构的核心价值是：

> **把模型能力转化为工程确定性。**

通过协议、运行时、控制、状态、输出、测试六层协同，SmartEats 从“可用”升级为“稳定好用、可观测、可持续优化”的生产级智能体链路。
